<% if current_user && selected_user %>
  <% if selected_user.child? %>
    <header>
      <h2>Habit Log</h2>
      <%= render 'habit_scores' %>
    </header>
    </br>
    <% selected_user.happenings.group_by_day(&:created_at).each do |group| %>
    </br>
      <span style="text-decoration: underline "><%= group.first %></span>
      <% group.second.each do |happening| %>
        <div>
          <%= happening.event_kind %> <%= happening.name %> <%= happening.point_value %>
          <small>(reported by <%= happening.reporting_user&.display_name %> at <%= happening.created_at.strftime("%H:%M") %>)</small>
        </div>
      <% end %>
    <% end %>
  <% end %>
<% end %>
